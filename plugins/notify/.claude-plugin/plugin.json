{
  "name": "notify",
  "version": "0.1.2",
  "description": "Push a phone notification (via ntfy.sh) when Claude Code stops, asks you a multiple-choice question, or hits a permission prompt. The title is '<session name>@<cwd>' and the body is Claude's latest reply. Bundles session-wide Stop / AskUserQuestion / PermissionRequest hooks; run /notify once to configure the ntfy topic.",
  "author": {
    "name": "jakobmusik"
  }
}
