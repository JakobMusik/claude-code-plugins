{
  "name": "notify",
  "version": "0.1.0",
  "description": "Push a phone notification (via ntfy.sh) when Claude Code stops, asks you a multiple-choice question, or hits a permission prompt. The notification body is the session name. Bundles session-wide Stop / AskUserQuestion / PermissionRequest hooks; run /notify once to configure the ntfy topic.",
  "author": {
    "name": "nyclanker"
  }
}
